import { Request, Response } from 'express';
import bcrypt from 'bcryptjs';
import jwt from 'jsonwebtoken';
import { Pool } from 'pg';
import asyncHandler from '../middlewares/asyncHandler';


                        // ====COMPANIES====
export const createCompany = (response:Response, request:Request) => {
    
}

export const getCompany = (response:Response, request:Request) => {

}

export const getAllCompanies = (response:Response, request:Request) => {

}

export const updateCompany = (response:Response, request:Request) => {
    
}

export const deleteCompany = (response:Response, request:Request) => {
    
}
                        // ====MANAGERS====

export const createManager = (response:Response, request:Request) => {
    
}

export const getManager = (response:Response, request:Request) => {

}

export const getAllManagers = (response:Response, request:Request) => {

}


export const updateManager = (response:Response, request:Request) => {
    
}

export const deleteManager = (response:Response, request:Request) => {
    
}

                        // ====EMPLOYEES====

export const createEmployee = (response:Response, request:Request) => {
    
}

export const getEmployee = (response:Response, request:Request) => {
    
}

export const getAllEmployees = (response:Response, request:Request) => {
    
}

export const updateEmployee = (response:Response, request:Request) => {
    
}

export const deleteEmployee = (response:Response, request:Request) => {
    
}

                        // ====DEPARTMENTS====
                        // ====TEAMS====
